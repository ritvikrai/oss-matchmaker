import './globals.css'
export const metadata = { title: 'OSS Matchmaker', description: 'Find your perfect open source contribution' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
